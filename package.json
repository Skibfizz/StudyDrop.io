{
  "name": "studymind-root",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "next": "14.1.0",
    "@supabase/ssr": "^0.5.2",
    "eslint": "^8.57.0",
    "glob": "^10.3.10",
    "rimraf": "^5.0.5",
    "lru-cache": "^10.2.0"
  },
  "scripts": {
    "build": "cd project && npm install && npm run build",
    "start": "cd project && npm start",
    "dev": "cd project && npm run dev"
  },
  "engines": {
    "node": ">=18.x"
  }
} 